import React from 'react';
import classes from '../todos.module.css';

const TodoPlaceholder = () => (
  <p className={classes.TodoPlaceholder}>You don't have any todos right now...</p>
);

export default TodoPlaceholder;
